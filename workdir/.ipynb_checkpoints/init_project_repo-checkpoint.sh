#! /usr/bin/env bash


# first time initialization


# create the github remote repo if not initialized

# initialize the local git repo

# add remote origin to the local repo

# add default gitignore file

# sync part 

# git add . && git commit -m 'auto commit' && git push origin master
